VITE_BACKEND_URL=http://localhost:3000

# Optional: path to an existing key/cert pair to serve HTTPS instead of the mkcert-generated one.
# Leave both empty to fall back to vite-plugin-mkcert.
SSL_KEY_FILE=
SSL_CRT_FILE=